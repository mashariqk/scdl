extern crate core;

pub mod configuration;
pub mod downloader;
pub mod executor;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
